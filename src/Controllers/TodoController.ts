import e , {Router, Request} from 'express';

const router = e.Router();

router.get('/', () => {});
router.get('/:id', () => {});
router.post('/', () => {});
router.patch('/:id', () => {});
router.delete('/:id', () => {});

export default router;