import {Router, Request} from 'express';

const router = Router();

router.get('/profile', () => {});
router.post('/signup', () => {});
router.patch('/', () => {});
router.delete('/signout/:id', () => {});

export default router;